const toggle=document.querySelector('.mobile-toggle');const nav=document.querySelector('.nav-links');if(toggle&&nav){toggle.addEventListener('click',()=>{nav.classList.toggle('open');toggle.setAttribute('aria-expanded',nav.classList.contains('open')?'true':'false')})}
const filters=document.querySelectorAll('.filter');const items=document.querySelectorAll('.filter-item');if(filters.length&&items.length){filters.forEach(btn=>btn.addEventListener('click',()=>{filters.forEach(b=>b.classList.remove('active'));btn.classList.add('active');const f=btn.dataset.filter;items.forEach(i=>i.classList.toggle('hidden',f!=='all'&&i.dataset.category!==f))}))}
function norm(s){return (s||'').toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'')}
const form=document.getElementById('site-search'),input=document.getElementById('search-input'),results=document.getElementById('search-results'),summary=document.getElementById('search-summary');
function renderSearch(q){if(!results||!summary)return;const query=norm(q).trim();if(!query){summary.textContent='Saisissez un mot-clé pour rechercher dans le portail.';results.innerHTML='';return}const words=query.split(/\s+/);const found=(window.SITE_INDEX||[]).filter(x=>{const hay=norm([x.title,x.type,x.text].join(' '));return words.every(w=>hay.includes(w))});summary.textContent=found.length?`${found.length} résultat(s) pour « ${q} »`:`Aucun résultat pour « ${q} »`;results.innerHTML=found.map(x=>`<a class="search-result" href="${x.url}"><span class="tag">${x.type}</span><h3>${x.title}</h3><p>${x.text}</p></a>`).join('')}
if(form&&input){form.addEventListener('submit',e=>{e.preventDefault();const q=input.value.trim();const u=new URL(location.href);u.searchParams.set('q',q);history.replaceState({},'',u);renderSearch(q)});const q=new URLSearchParams(location.search).get('q')||'';input.value=q;renderSearch(q)}
